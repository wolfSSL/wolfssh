name: wolfssh
description: Dart FFI bindings for the wolfSSH client (host-key verification mandatory).
version: 0.1.0-dev
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  ffigen: ^14.0.0
  test: ^1.25.0
  lints: ^4.0.0
